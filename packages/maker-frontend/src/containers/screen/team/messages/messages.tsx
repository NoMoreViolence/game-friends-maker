import React, { FC } from 'react';
import { ScrollContainer, Container } from 'ui';

export const Messages: FC = () => {
  return (
    <Container>
      <ScrollContainer>messages</ScrollContainer>
    </Container>
  );
};
