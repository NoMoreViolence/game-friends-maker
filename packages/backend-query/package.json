{
  "name": "backend-query",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/typescript/bin/tsc",
    "prod_clean_without_user": "yarn build && NODE_ENV=prod node dist/index.js clean_withoutuser",
    "dev_clean_without_user": "yarn build && NODE_ENV=dev node dist/index.js clean_withoutuser",
    "prod_game_create": "yarn build && NODE_ENV=prod node dist/index.js game create",
    "prod_game_update": "yarn build && NODE_ENV=prod node dist/index.js game update",
    "prod_game_delete": "yarn build && NODE_ENV=prod node dist/index.js game delete",
    "dev_game_create": "yarn build && NODE_ENV=dev node dist/index.js game create",
    "dev_game_update": "yarn build && NODE_ENV=dev node dist/index.js game update",
    "dev_game_delete": "yarn build && NODE_ENV=dev node dist/index.js game delete",
    "prod_genre_create": "yarn build && NODE_ENV=prod node dist/index.js genre create",
    "prod_genre_update": "yarn build && NODE_ENV=prod node dist/index.js genre update",
    "prod_genre_delete": "yarn build && NODE_ENV=prod node dist/index.js genre delete",
    "dev_genre_create": "yarn build && NODE_ENV=dev node dist/index.js genre create",
    "dev_genre_update": "yarn build && NODE_ENV=dev node dist/index.js genre update",
    "dev_genre_delete": "yarn build && NODE_ENV=dev node dist/index.js genre delete"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "mongodb": "^3.5.6",
    "mongoose": "^5.9.9",
    "mongoosejs-soft-delete": "^1.0.2",
    "reflect-metadata": "^0.1.13",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/mongoose": "^5.7.12",
    "prettier": "^2.0.4"
  }
}
